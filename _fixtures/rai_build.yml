rai:
  version: 0.2 # this is required
  image: gcc
resources:
  # gpus: 1 # currently required to be 1
commands:
  build:
    - echo "Building project"
    - /bin/sh -c ls /usr/local/bin
    - /usr/local/bin/gcc /src/main.c
    - ./a.out
